{
  "name": "yana-web",
  "private": true,
  "version": "0.1.0",
  "description": "Browser client for YANA/. Built with esbuild and embedded into the yana binary.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit",
    "test": "tsc --noEmit"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.6.0"
  }
}
