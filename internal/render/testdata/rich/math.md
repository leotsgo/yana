Inline: $E = mc^2$ and $\alpha_1 + \beta_2$ in a sentence.

Prices stay prices: it costs $5 and $10, or $5-$10 in bulk, and `$HOME` is $HOME.

A bare $ sign, a spaced $ x $ pair, and a closing$ that runs into $1 million.

Escaped: \$not math\$.

Across lines is not inline: $a
b$.

$$
\int_0^1 x^2 \, dx = \frac{1}{3}
$$

$$ a < b $$

$$
\begin{aligned}
x &= 1 \\
y &= 2
\end{aligned}
$$

> [!info] In a callout
> $$
> \sum_{i=1}^n i
> $$

- A list item with $x_i$ math.

$$ unclosed on this line
